pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'building the applications'
                echo 'building the app'
            }
        }

        stage("test") {
            steps {
                echo 'testing the app'
            }
        }

        stage("deploy") {
            steps {
                echo 'deploy the app'
            }
        }
    }
}